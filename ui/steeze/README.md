# Steeze UI System

This directory is reserved for shared UI code when UI work is added to this
repo. Follow the structure and documentation rules in:

- `docs/steeze-ui-system.md`

Components must be documented under `ui/steeze/docs/`.
